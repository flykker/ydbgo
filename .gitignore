# binaries
/ydbgo
/dist/

# data directories (standalone + per-node n1..nN)
/data/
/data*/
/n*/

# pebble + raft state (already under data dirs, kept here for safety)
*.pebble
/raft/
/stable

# OS / editor
.DS_Store
*.swp